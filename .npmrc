# Hoist React and related packages to root node_modules
# This ensures all packages use the same React instance
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*@types/react*
shamefully-hoist=true
